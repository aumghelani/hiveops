{
  "name": "hiveops",
  "private": true,
  "version": "0.1.0",
  "description": "HiveOps — Enterprise AI incident resolution",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && .venv\\Scripts\\uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npx vite --port 5173",
    "build": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install && cd ../backend && python -m venv .venv && .venv/Scripts/pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
